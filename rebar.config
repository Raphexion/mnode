%% -*- mode: erlang -*-

{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, {mnode, "0.0.1"},
	 [mnode]},
	{dev_mode, true},
	{include_erts, true},

	{extended_start_script, true}]}.

{shell, [
  % {config, [{config, "config/sys.config"}]},
    {apps, [mnode]}
]}.
